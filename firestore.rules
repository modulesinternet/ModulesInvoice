rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Allow read & write on all collections to let the secure backend proxy persist operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
